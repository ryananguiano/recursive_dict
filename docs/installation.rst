============
Installation
============

At the command line::

    $ easy_install recursive_dict

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv recursive_dict
    $ pip install recursive_dict
